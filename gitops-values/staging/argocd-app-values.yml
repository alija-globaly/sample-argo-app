server:
  service:
    type: NodePort
    # argo-cd Helm chart expects nodePortHttp / nodePortHttps (not `nodePort`)
    nodePortHttp: 30081

  extraArgs:
    - --insecure

  ingress:
    enabled: false

configs:
  params:
    server.insecure: true